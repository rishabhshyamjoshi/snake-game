//class Snake {
  //constructor() {
  
  //  this.body = Bodies.rctangle(this.body.position.x,this.body.position.y,this.width,this.height);

   // this.xdir = 0;
   // this.ydir = 0;
   // this.len = 0;

   // World.add(world,this.body);
//  }
  //eat(pos) {
  //  if(x == pos.x && y == pos.y) {
 ///     this.grow();
     // return true;
  //  }
 // }
 ////   setDir(x, y) {
  ///    this.xdir = x;
   //   this.ydir = y;
 //   }
   // diplay(){

   /// }
//}